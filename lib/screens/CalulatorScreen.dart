import 'package:flutter/material.dart';

class CalulatorScreen extends StatelessWidget {
const CalulatorScreen({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context){
    return Scaffold(
      body: Container(
          
      ),
    );
  }
}